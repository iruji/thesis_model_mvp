<?php

/**
 * Pattern definitions for intent detection
 * Reserved for CLARE v1.0
 *
 * When PatternMatcher is refactored to load externally,
 * patterns will live here instead of inside the class.
 * This allows adding new patterns without touching class logic.
 *
 * Structure:
 * [
 *     'pattern'    => '/regex/i',
 *     'intent'     => 'intent_name',
 *     'confidence' => 0.0 to 1.0,
 *     'extractor'  => 'methodName',  // optional
 * ]
 */

return [];
